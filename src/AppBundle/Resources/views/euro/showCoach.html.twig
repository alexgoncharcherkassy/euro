{% extends 'base.html.twig' %}

{% block body %}
    {{ parent() }}

    <div class="container">
        <div class="jumbotron">
            <p>
                Coach:  {{ coaches.firstName[0] }} {{ coaches.lastName[0] }}
            </p>
            <p>
                Birth Day: {{ coaches.birthDay[0]|date('d-m-Y') }}
            </p>
            <p>
                {{ coaches.biography[0] }}
            </p>

        </div>
    </div> <!-- /container -->

{% endblock %}

